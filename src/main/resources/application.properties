spring.application.name=sql

# Server port
server.port=9001
server.forward-headers-strategy=framework

# MySQL datasource
spring.datasource.url=jdbc:mysql://localhost:3306/app_fac?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=Amypo
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect


# ==============================
# 🔐 JWT SECURITY (ADD THESE)
# ==============================
# Secret Key for JWT
app.jwt-secret=MY_SUPER_SECRET_KEY_123456

# Expiry time of token (1 day = 86400000 ms)
app.jwt-expiration-ms=86400000
